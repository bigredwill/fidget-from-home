[tool.poetry]
name = "rpi-client"
version = "0.1.0"
description = ""
authors = ["wills <will@simons.dev>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11.9"
websockets = "^12.0"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
